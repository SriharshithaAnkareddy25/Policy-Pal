import { Download, FileText, Filter, MoreHorizontal, Search, SlidersHorizontal } from "lucide-react";
import { documents } from "@/src/constants/mock-data";
import { Card } from "@/src/components/ui/card";
import { Input } from "@/src/components/ui/input";
export default function Documents(){return <div className="mx-auto max-w-7xl"><PageTitle title="Documents" copy="Manage and review every document in your workspace."/><Card className="mt-7 overflow-hidden"><div className="flex flex-col gap-3 border-b border-navy/10 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-stone" size={16}/><Input className="pl-9" placeholder="Search documents"/></div><button className="filter-button"><Filter size={16}/>Type</button><button className="filter-button"><SlidersHorizontal size={16}/>Sort</button></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left"><thead><tr className="border-b border-navy/10 bg-surface text-[11px] uppercase text-stone"><th className="table-cell">Document name</th><th className="table-cell">Type</th><th className="table-cell">Upload date</th><th className="table-cell">Status</th><th className="table-cell"><span className="sr-only">Actions</span></th></tr></thead><tbody>{documents.map(doc=><tr key={doc.name} className="border-b border-navy/8 last:border-0 hover:bg-surface/60"><td className="table-cell"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-md bg-[#fff0ed] text-red"><FileText size={17}/></span><span className="font-semibold text-navy">{doc.name}</span></div></td><td className="table-cell text-stone">{doc.type}</td><td className="table-cell text-stone">{doc.date}</td><td className="table-cell"><span className={doc.status==="Ready"?"ready-pill":"processing-pill"}>{doc.status}</span></td><td className="table-cell"><div className="flex justify-end gap-1"><button className="icon-button" aria-label={`Download ${doc.name}`}><Download size={16}/></button><button className="icon-button" aria-label={`More options for ${doc.name}`}><MoreHorizontal size={16}/></button></div></td></tr>)}</tbody></table></div></Card></div>}
function PageTitle({title,copy}:{title:string;copy:string}){return <div><p className="text-xs font-bold uppercase text-tangerine">Document library</p><h1 className="mt-1 text-3xl font-semibold text-navy">{title}</h1><p className="mt-2 text-sm text-stone">{copy}</p></div>}
