import { CalendarDays, Filter, Search } from "lucide-react";
import { history } from "@/src/constants/mock-data";
import { Card } from "@/src/components/ui/card";
import { Input } from "@/src/components/ui/input";
export default function History(){return <div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase text-tangerine">Audit trail</p><h1 className="mt-1 text-3xl font-semibold text-navy">Analysis history</h1><p className="mt-2 text-sm text-stone">Revisit previous questions, decisions, and confidence scores.</p><Card className="mt-7 overflow-hidden"><div className="flex flex-col gap-3 border-b border-navy/10 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-stone" size={16}/><Input className="pl-9" placeholder="Search questions or documents"/></div><button className="filter-button"><CalendarDays size={16}/>Date</button><button className="filter-button"><Filter size={16}/>Decision</button></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left"><thead><tr className="border-b border-navy/10 bg-surface text-[11px] uppercase text-stone">{["Date","Document","Question","Decision","Confidence"].map(h=><th className="table-cell" key={h}>{h}</th>)}</tr></thead><tbody>{history.map(row=><tr key={row.question} className="border-b border-navy/8 last:border-0 hover:bg-surface/60"><td className="table-cell whitespace-nowrap text-stone">{row.date}</td><td className="table-cell font-semibold text-navy">{row.document}</td><td className="table-cell max-w-sm text-ink">{row.question}</td><td className="table-cell"><span className="decision-pill">{row.decision}</span></td><td className="table-cell"><div className="flex items-center gap-2"><div className="h-1.5 w-20 overflow-hidden rounded-full bg-navy/8"><div className="h-full bg-tangerine" style={{width:`${row.confidence}%`}}/></div><span className="text-xs font-semibold text-navy">{row.confidence}%</span></div></td></tr>)}</tbody></table></div></Card></div>}
